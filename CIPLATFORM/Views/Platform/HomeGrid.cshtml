@using CI_PLATFORM.Entities.ViewModels
@model CardsViewModel


<link rel="stylesheet" href="~/css/HomeGrid.css">


@if (TempData["logins"] != null)
{
    <script type="text/javascript">
        toastr.success('@TempData["logins"]')
    </script>
}


<hr class="d-none d-md-block">

@{
    Html.RenderPartial("_navbar2");
}

<hr>
<div class="container">
    <div class="row">
        <div class="col-md-9 col-sm-12"></div>
        <div class="col-md-3 col-sm-12 d-flex justy">
            <div class="dropdown h-dropdown">
                <select class="form-select w-100 d-inline-flex" aria-label="Default select example" onchange="temp()" id="sort">
                    <option selected>Sort by</option>
                    <option value="1">New to Old</option>
                    <option value="2">Old to New</option>
                    <option value="3">Favorite Mission</option>
                </select>
            </div>
            <div class="">
                <a onclick="opengrid()"><i class="bi bi-grid"></i></a>
            </div>
            <div class="">
                <a onclick="openlist()"><i class="bi bi-list"></i></a>
            </div>
        </div>
    </div>
</div>

<div class="container">

    @{
        Html.RenderPartial("_FilterMission");
    }

</div>

